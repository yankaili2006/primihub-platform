# 白名单404问题 - 最终解决方案

## 问题根因

**Clash代理配置导致容器间通信失败**

Docker Daemon的代理配置（`~/.docker/config.json`）中的`noProxy`列表不包含内部容器名称，导致Nginx容器尝试通过代理（127.0.0.1:7890）访问Gateway容器时失败。

## 解决方案

### 1. 修复Docker代理配置

编辑 `~/.docker/config.json`：

```json
{
  "proxies": {
    "default": {
      "httpProxy": "http://127.0.0.1:7890",
      "httpsProxy": "http://127.0.0.1:7890",
      "noProxy": "*"  // ← 禁用容器内的所有代理
    }
  }
}
```

### 2. 重新创建所有容器

```bash
cd ~/github/primihub-deploy/docker-all-in-one

# 停止并删除受影响的容器
docker stop manage-web0 manage-web1 manage-web2 gateway0 application0
docker rm manage-web0 manage-web1 manage-web2 gateway0 application0

# 重新创建所有服务
docker compose up -d
```

### 3. 清除用户登录token

```bash
docker exec redis redis-cli -a primihub KEYS "sys_user:login_token_*" | \
  xargs -I {} docker exec redis redis-cli -a primihub DEL {}
```

## 验证结果

✅ API链路完全打通：

```bash
$ curl "http://100.64.0.23:30811/prod-api/whitelist/findWhitelistPage?pageNum=1&pageSize=10&timestamp=xxx&nonce=xxx&token=xxx"
{"msg":"token失效","code":102}  # 正确的业务错误，说明链路通了
```

## 用户操作步骤

**请立即执行以下操作：**

### 步骤1：退出登录
点击右上角头像 → 退出登录

### 步骤2：清除浏览器缓存
- **Chrome/Edge**: 按 `Ctrl + Shift + Delete` → 选择"缓存的图片和文件" → 清除数据
- **Safari**: 按 `Cmd + Shift + Delete` → 清除缓存
- 或**强制刷新**: `Ctrl + Shift + R` (Mac: `Cmd + Shift + R`)

### 步骤3：重新登录
使用admin账号重新登录系统

### 步骤4：测试新增菜单
访问以下菜单，确认都能正常访问：
- ✓ 系统设置 → 白名单列表
- ✓ 系统设置 → 租户管理
- ✓ 系统设置 → 存证管理
- ✓ 系统设置 → 监控管理
- ✓ 系统设置 → 接口管理

## 预期结果

登录成功后，访问白名单列表应该看到：

```json
{
  "code": 0,
  "msg": "请求成功",
  "result": {
    "list": [
      {"id": 1, "type": "IP", "value": "192.168.1.100", "description": "办公网IP"},
      {"id": 2, "type": "IP", "value": "10.0.0.0/24", "description": "内网IP段"},
      {"id": 3, "type": "DOMAIN", "value": "primihub.com", "description": "公司域名"},
      {"id": 4, "type": "USER_ID", "value": "1001", "description": "测试用户"}
    ],
    "pageParam": {"pageNum": 1, "pageSize": 10, "itemTotalCount": 4}
  }
}
```

## 如果仍然出现404

### 检查1：确认已重新登录

开发者工具(F12) → Application → Cookies → 查看token是否是新的

### 检查2：查看完整错误

开发者工具(F12) → Network → 点击失败的请求 → Response → 截图

### 检查3：验证代理配置

```bash
docker exec manage-web0 env | grep proxy
# 应该看到: no_proxy=*
```

### 检查4：验证容器连接

```bash
docker exec manage-web0 curl -s http://gateway0:8080/test
# 不应该看到 "connect to 127.0.0.1 port 7890 failed"
```

## 技术总结

整个问题涉及3个层面的修复：

### 1. 数据库层面
- ✅ auth_url配置为后端API路径（不是前端路由）
- ✅ 权限已分配给admin角色

### 2. 应用层面
- ✅ WhitelistController等新增Controller已编译部署
- ✅ Gateway路由配置包含whitelist-url-proxy
- ✅ Redis权限缓存已刷新

### 3. 基础设施层面（关键！）
- ✅ Docker代理配置修复（noProxy=*）
- ✅ 所有容器重新创建（不是重启）
- ✅ 容器间网络通信恢复正常

## 核心教训

**容器内部不应该有代理配置！**

- 代理适用于：容器访问外部互联网
- 代理不适用于：容器间内部通信

配置原则：
```json
{
  "httpProxy": "http://127.0.0.1:7890",  // 用于外网访问
  "noProxy": "*"  // 禁用容器内所有代理，让内部通信直连
}
```

## 口诀

**代理设宿主，容器须直连；**
**数据库配对，权限要分全；**
**缓存需清空，容器要重建；**
**用户重登录，问题即解决！**

---

修复完成时间：2026-01-10 10:07
修复人员：Claude Code
验证状态：✅ API链路已打通，等待用户重新登录验证
