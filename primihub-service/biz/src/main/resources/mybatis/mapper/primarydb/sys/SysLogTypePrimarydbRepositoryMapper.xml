<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.primihub.biz.repository.primarydb.sys.SysLogTypePrimarydbRepository">
    <insert id="insertLogType" parameterType="com.primihub.biz.entity.sys.po.SysLogType">
        INSERT INTO sys_log_type (type_name, type_code, type_desc, status, sort_order)
        VALUES (#{typeName}, #{typeCode}, #{typeDesc}, #{status}, #{sortOrder})
    </insert>
    <update id="updateLogType" parameterType="com.primihub.biz.entity.sys.po.SysLogType">
        UPDATE sys_log_type SET type_name=#{typeName}, type_code=#{typeCode},
               type_desc=#{typeDesc}, status=#{status}, sort_order=#{sortOrder}
        WHERE id=#{id}
    </update>
    <update id="deleteLogType">UPDATE sys_log_type SET is_del=1 WHERE id=#{id}</update>
</mapper>