<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.primihub.biz.repository.primarydb.sys.SysDataExchangeLogPrimarydbRepository">
    <insert id="insert" parameterType="com.primihub.biz.entity.sys.po.SysDataExchangeLog">
        INSERT INTO sys_data_exchange_log (exchange_type,exchange_name,source_organ,target_organ,data_size,sync_status,trigger_type)
        VALUES (#{exchangeType},#{exchangeName},#{sourceOrgan},#{targetOrgan},#{dataSize},0,'manual')
    </insert>
    <update id="updateSyncStatus">UPDATE sys_data_exchange_log SET sync_status=#{syncStatus}, sync_msg=#{syncMsg} WHERE id=#{id}</update>
    <update id="delete">UPDATE sys_data_exchange_log SET is_del=1 WHERE id=#{id}</update>
</mapper>