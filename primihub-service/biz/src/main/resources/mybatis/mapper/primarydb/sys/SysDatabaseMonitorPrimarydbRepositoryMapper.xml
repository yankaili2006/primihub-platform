<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.primihub.biz.repository.primarydb.sys.SysDatabaseMonitorPrimarydbRepository">
    <insert id="insert" parameterType="com.primihub.biz.entity.sys.po.SysDatabaseMonitorConfig">
        INSERT INTO sys_database_monitor_config (db_type,db_name,host,port,db_user,db_password,connect_timeout,warning_threshold,critical_threshold,check_interval,enabled,notify_type,remark)
        VALUES (#{dbType},#{dbName},#{host},#{port},#{dbUser},#{dbPassword},#{connectTimeout},#{warningThreshold},#{criticalThreshold},#{checkInterval},#{enabled},#{notifyType},#{remark})
    </insert>
    <update id="update" parameterType="com.primihub.biz.entity.sys.po.SysDatabaseMonitorConfig">
        UPDATE sys_database_monitor_config SET db_type=#{dbType},db_name=#{dbName},host=#{host},port=#{port},
            db_user=#{dbUser},db_password=#{dbPassword},connect_timeout=#{connectTimeout},
            warning_threshold=#{warningThreshold},critical_threshold=#{criticalThreshold},
            check_interval=#{checkInterval},enabled=#{enabled},notify_type=#{notifyType},remark=#{remark}
        WHERE id=#{id}
    </update>
    <update id="delete">UPDATE sys_database_monitor_config SET is_del=1 WHERE id=#{id}</update>
</mapper>